# Code Style Generator Skill

自动检测项目代码规范，生成个人代码习惯文档 `CODE_STYLE.md`。

## 核心特性

- ✅ **自动检测**：从配置文件和代码推断编码规范
- ✅ **智能推断**：扫描项目代码，推断命名规范、引号习惯等
- ✅ **用户确认**：生成探测代码片段，用户快速确认或修改
- ✅ **补充提问**：只针对无法自动检测的规则提问
- ✅ **多框架支持**：支持 Vue、React、原生 JavaScript 项目
- ✅ **优先级合并**：智能合并不同来源的规则

## 快速开始

### 触发方式

使用自然语言描述需求：

```
生成我的代码习惯文档
创建代码风格规范文档
生成 CODE_STYLE.md
检测项目代码规范
```

### 执行流程

```
用户输入
    ↓
检测配置文件 (.editorconfig, .prettierrc, eslint)
    ↓
扫描代码推断规范 (naming, quotes, style)
    ↓
生成探测代码片段
    ↓
用户确认与补充
    ↓
生成 CODE_STYLE.md
```

## 文件结构

```
code-style-generator/
├── SKILL.md                          # 主skill文档
├── README.md                         # 使用说明
├── EXAMPLES.md                       # 详细使用示例
└── atomic-skills/
    ├── detect-config-rules/
    │   └── SKILL.md                 # 检测配置文件规则
    ├── scan-code-patterns/
    │   └── SKILL.md                 # 扫描代码推断规则
    ├── generate-probe-code/
    │   └── SKILL.md                 # 生成探测代码
    ├── confirm-and-supplement/
    │   └── SKILL.md                 # 用户确认与补充
    └── generate-style-document/
        └── SKILL.md                 # 生成最终文档
```

## 检测的规则项

### 配置文件规则（自动检测）

| 规则 | 配置文件 | 检测方式 |
|------|---------|---------|
| 缩进 | .editorconfig, .prettierrc | 直接读取 |
| 引号 | .prettierrc, eslint | 直接读取 |
| 分号 | .prettierrc, eslint | 直接读取 |
| 行长度 | .editorconfig, .prettierrc | 直接读取 |
| 尾逗号 | .prettierrc | 直接读取 |
| 文件末尾换行符 | .editorconfig | 直接读取 |

### 代码推断规则（自动推断）

| 规则 | 推断方法 |
|------|---------|
| HTML属性引号 | 统计 HTML 标签属性中的引号使用频率 |
| 组件命名规范 | 分析文件名（PascalCase/kebab-case） |
| 变量命名规范 | 分析变量名（camelCase/snake_case） |
| CSS类名命名 | 分析 CSS 类名（kebab-case/BEM） |
| const/let使用 | 统计 const 和 let 的使用比例 |
| 箭头函数风格 | 检测箭头函数是否总是使用大括号 |
| Vue Script风格 | 检测是否使用 `<script setup>` |
| React组件风格 | 检测组件定义方式 |
| CSS单位偏好 | 统计 px 和 rem 的使用频率 |

### 用户确认规则（交互式提问）

| 规则 | 提问方式 |
|------|---------|
| 函数空行规则 | AskUserQuestion (选项) |
| 业务模块注释格式 | AskUserQuestion (选项) |
| 条件渲染偏好 | AskUserQuestion (选项) |
| 列表渲染方式 | AskUserQuestion (选项) |
| 注释风格 | AskUserQuestion (选项) |
| 调试代码处理 | AskUserQuestion (选项) |

## 规则优先级

按以下优先级合并规则（高优先级覆盖低优先级）：

1. **用户确认**：明确确认的规则（最高优先级）
2. **用户修改**：用户手动修改探测代码的规则
3. **配置文件**：从配置文件检测到的规则
4. **代码推断**：从代码扫描推断的规则（最低优先级）

## 输出文件

**文件名**：`CODE_STYLE.md`
**位置**：项目根目录

### 文档结构

```markdown
# 代码习惯规范

## 基础格式
- 缩进
- 引号
- 分号
- 大括号
- 空格
- 行长度
- 尾逗号
- 文件末尾

## JavaScript
- 变量声明
- 箭头函数
- 命名风格
- 注释风格

## Vue / React（根据项目类型）
- 组件定义
- Hooks导入
- Props解构
- 条件渲染
- 列表渲染

## CSS
- 命名风格
- 单位偏好

## 代码示例
- HTML示例
- CSS示例
- JavaScript示例
- Vue示例（如适用）
- React示例（如适用）

## 总结表格
```

## 使用示例

### 示例1：基础使用

```
用户：生成我的代码习惯文档

系统：检测到配置文件...
      扫描代码文件...
      生成探测代码...

      请查看以下代码片段，确认是否符合你的习惯：
      [探测代码]

      如果代码不符合你的习惯，请直接修改代码。
      如果符合你的习惯，请回复"确认"。

用户：[查看并修改代码] 或 [确认]

系统：需要补充确认几个问题：
      1. 函数空行规则？
      2. 业务模块注释格式？
      3. 注释风格？

用户：[回答问题]

系统：生成文件：CODE_STYLE.md
      规则来源：配置文件[8]，代码推断[12]，用户确认[5]
```

### 示例2：用户修改探测代码

```
用户修改了探测代码：
// 原始
const name = 'single quote'
const age = "double quote"

// 用户修改
const name = 'single quote'
const age = 'double quote'  // 改为单引号

系统：检测到用户修改：
      - 引号规则：统一使用单引号
      规则优先级：用户修改 > 配置文件
      已更新规则列表。
```

## 优势对比

### 相比纯提问方式

| 方案 | 问题数量 | 用户负担 | 准确性 |
|------|---------|---------|--------|
| 纯提问方式 | 20+ | 高 | 依赖用户记忆 |
| 本方案 | 3-5 | 低 | 基于实际代码 |

### 相比纯代码检测

| 方案 | 交互性 | 灵活性 | 覆盖率 |
|------|--------|--------|--------|
| 纯代码检测 | 无 | 低 | 无法检测偏好规则 |
| 本方案 | 有 | 高 | 覆盖所有规则类型 |

## 技术细节

### 文件扫描策略

- 每种文件类型最多扫描 3 个文件
- 扫描目录：`src/`, `lib/`, `app/`
- 支持的文件类型：`.js`, `.jsx`, `.ts`, `.tsx`, `.vue`, `.css`, `.less`, `.scss`

### 置信度计算

```
置信度 = (匹配次数 / 总次数) × 100%

高置信度：> 80%
中置信度：60% - 80%
低置信度：50% - 60%
不可靠：< 50%
```

### 规则冲突处理

```
冲突示例：
- 配置文件：单引号
- 代码推断：双引号（55%）

处理：采用配置文件规则，在探测代码中标注冲突
```

## 扩展性

### 添加新的检测规则

1. 在 `detect-config-rules/SKILL.md` 中添加配置文件规则
2. 在 `scan-code-patterns/SKILL.md` 中添加推断逻辑
3. 在 `generate-probe-code/SKILL.md` 中添加探测代码
4. 更新文档模板

### 支持新的框架

1. 在 `scan-code-patterns/SKILL.md` 中添加框架检测逻辑
2. 在 `generate-probe-code/SKILL.md` 中添加框架探测代码
3. 在 `confirm-and-supplement/SKILL.md` 中添加框架相关问题

## 最佳实践

1. **配置文件优先**：建议项目使用 `.editorconfig` 和 `.prettierrc`，可大幅减少提问
2. **代码一致性**：保持代码风格一致，提高推断准确性
3. **及时更新**：修改编码习惯后，重新生成文档
4. **版本控制**：将 `CODE_STYLE.md` 加入 Git，方便团队同步

## 常见问题

### Q1: 为什么有些规则无法自动检测？

A: 某些规则属于"偏好"或"习惯"，无法从代码中推断，例如：
- 函数空行规则（业务逻辑分组）
- 注释风格（JSDoc/简洁）
- 调试代码保留规则

### Q2: 如果我对探测代码不满意怎么办？

A: 可以直接修改探测代码，系统会自动识别你的修改并更新规则。

### Q3: 可以同时支持 Vue 和 React 吗？

A: 可以。系统会检测项目中的文件类型，同时生成两种框架的规范。

### Q4: 生成的文档可以手动修改吗？

A: 可以。但建议修改后重新运行 skill，保持文档与代码一致性。

## 更多示例

查看 [EXAMPLES.md](./EXAMPLES.md) 获取详细使用示例。

---

*Author: Claude Code*
*Version: 1.0.0*
