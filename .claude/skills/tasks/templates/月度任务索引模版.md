# {YYYY-MM}月度任务索引

## 索引说明
本索引用于记录{MM}月所有归档任务，支撑Reasoner历史意图检索，不记录任务详情，仅保留核心检索信息。

## 当月任务列表（按归档时间排序）
| 任务ID | 归档时间 | 用户意图（核心） | 归属主skill | 任务结果（摘要） | 相似度特征（供检索） | 归档路径 |
|--------|----------|------------------|-------------|------------------|----------------------|----------|
| {任务ID1} | {YYYY-MM-DD HH:MM:SS} | {核心意图摘要} | {主skill名称} | {结果摘要，10字以内} | {特征关键词，用逗号分隔} | history/{YYYY-MM}/task_skill_{任务ID1}.md |
| {任务ID2} | {YYYY-MM-DD HH:MM:SS} | {核心意图摘要} | {主skill名称} | {结果摘要，10字以内} | {特征关键词，用逗号分隔} | history/{YYYY-MM}/task_skill_{任务ID2}.md |
| ...    | ...      | ...              | ...         | ...              | ...                  | ...      |

## 索引更新说明
1.  新增归档任务时，自动在列表末尾添加对应条目，不修改原有条目；
2.  索引仅记录已归档任务，未归档（current下）任务不录入；
3.  相似度特征需与步骤（2.1）一致，便于Reasoner快速检索。