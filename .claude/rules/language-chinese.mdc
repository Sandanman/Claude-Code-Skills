---
name: language-chinese
description: 所有自然语言回复全程使用中文，技术术语可保留英文原文
alwaysApply: true
---

# 语言规则：全程使用中文

## 一、适用范围

| 场景 | 是否需要中文 |
|------|------------|
| 思考过程、推理分析 | ✅ 必须 |
| 用户可见的回复内容 | ✅ 必须 |
| 工具调用说明 | ✅ 必须 |
| 文件内容（代码/配置/日志等） | ❌ 不需要，保留原样 |
| 命令行输出（bash / git 等） | ❌ 不需要，保留原样 |
| 技术错误信息 | ❌ 不需要，保留原样 |
| 代码注释 / 变量名 / 文件名 | ❌ 不需要，按项目规范 |

**无论项目语言、文件内容、用户提问语言，均强制执行。**

## 二、具体要求

### 必须使用中文的场景

- 内部推理与思考过程（分析步骤、方案取舍、原因说明）
- 回答内容（代码说明、操作指引、错误原因、建议与结论）
- 工具操作的描述（做了什么、为什么这样做）
- 提交信息的描述部分
- skill 生成的文档、变更日志、README 等自然语言内容

### 允许保留英文的场景

- 技术术语的标准名称（如：WebSocket、RESTful API、CI/CD）
- 变量名、函数名、文件名、路径（保留原始命名）
- 代码块内的注释（按项目规范，参见本文件的第三条）
- 工具返回的原始内容（bash 输出、API 响应、JSON 等）

### 技术术语处理

常用术语如遇歧义，应在首次出现时括号标注中文：

```
WebRTC（网页实时通信）、CI/CD（持续集成/持续部署）、
SSR（服务端渲染）、SSG（静态站点生成）、SEO（搜索引擎优化）、
i18n（国际化）、l10n（本地化）
```

## 三、代码注释说明

代码注释遵循**本规则约束**，但同时遵守项目规范：

| 项目规范 | AI 操作方式 |
|----------|------------|
| 注释用英文 | 生成英文注释（因为是对项目规范的遵守） |
| 注释用中文 | 生成中文注释（满足本规则 + 项目规范一致） |
| 无显式规范 | 默认生成中文注释 |

**原则**：本规则的优先级低于项目规范 — 如果项目有明确要求，按项目要求执行。

## 四、边界场景处理

| 场景 | 处理方式 |
|------|---------|
| 用户用英文提问 | 用中文回答（不跟随用户语言） |
| 用户直接要求用英文回复 | 拒绝，明确说明按语言规则必须使用中文 |
| 代码中包含多语言注释 | 以项目原始语言为准，不做翻译 |
| 文件名 / 路径含中文 | 保留原样，不翻译 |
| GitHub / GitLab 等外部服务的英文内容 | 描述操作步骤用中文，保留引用内容原文 |

## 五、示例

### ✅ 正确

```
我先检查一下 webRTC.js 中的 createOffer 逻辑……
发现 SDP 交换流程缺少了候选地址的处理……
建议将定时器逻辑移到 onBeforeUnmount 中清理……
```

### ❌ 错误

```
First, check the createOffer logic in webRTC.js, then handle SDP exchange...
I'll analyze this step by step...
The issue is caused by a race condition.
```

## 六、语言检测

- **中文**：简体/繁体均接受，按项目语言习惯走
- **代码块内的自然语言**：同本规则约束
- **多语言混合项目**：本规则约束 AI 输出，代码内容按原样保留