---
name: project-workflow
description: 项目协作行为规范，任务执行和 Git 操作的行为约束
alwaysApply: true
---

## 一、Git 提交规范

### 禁止自动提交

**规则：禁止在任务完成后主动建议或执行 git commit，必须由用户明确要求。**

```
触发条件：任意 Skill 任务完成后（无论成功/失败/部分完成）

禁止行为：
- ❌ 主动建议"要提交代码吗？"
- ❌ 主动执行 git commit
- ❌ 在结果输出中提示"建议提交"
- ❌ 任务结束后自动触发 git-assistant

必须行为：
- ✅ 完成任务后正常输出结果
- ✅ 仅当用户明确要求（"提交"、"commit"、"帮我提交"）时才执行提交
- ✅ 如需提示下一步，可提示"如需提交代码请告诉我"

原因：用户可能在多个文件中做了不相关的修改，冒然提交会污染提交历史。
每次提交应由用户自行判断和触发。
```

### 提交触发条件（例外）

仅在以下**明确场景**下可主动提及提交：
1. 用户刚完成合并（merge）或变基（rebase）操作，有未提交的冲突解决
2. 用户显式要求"保存当前进度"或类似表述
3. 用户明确表示"做完了，提交吧"类意图

---

## 二、任务完成行为规范

### 结果输出

任务完成后，只输出执行结果摘要，**不主动提供后续行动建议**（除非用户询问）。

```
禁止：
- ❌ "建议运行测试验证"
- ❌ "可以提交代码了"
- ❌ "接下来可以部署"

允许（仅在用户询问时）：
- ✅ "需要我帮你提交代码吗？"
- ✅ "需要运行测试吗？"
```

### 任务归档

- 任务完成后写入 `tasks/current/task_skill.md` 归档记录
- 不主动迁移任何文件到项目目录（需用户确认）
- 不主动修改 CHANGELOG、版本号等版本相关文件

---

**版本**: 1.0
**创建时间**: 2026-06-03
**规则性质**: alwaysApply（所有会话自动加载）