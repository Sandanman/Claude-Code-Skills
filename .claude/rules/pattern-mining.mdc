---
name: pattern-mining
description: Pattern Mining 规则 — 复用触发、成熟度评估、模式库管理
alwaysApply: true
---

## 一、复用触发规则

**pattern-001**: 相似度 >= 0.6 时，优先复用历史模式，而非重新执行
**pattern-002**: 成熟模式（出现 >= 5 次）τ 折扣 70%
**pattern-003**: 成长模式（出现 2-4 次）τ 折扣 40%
**pattern-004**: 试验模式（出现 1 次）τ 折扣 10%
**pattern-005**: 新建模式无折扣

## 二、模式评估规则

**pattern-010**: 复用率 = 已匹配模式数 / 总步骤数
**pattern-011**: 复用率 < 30% 时，建议触发新模式挖掘
**pattern-012**: 新建模式必须经过 2 次验证才能晋升为成长模式
**pattern-013**: 成长模式必须经过 3 次验证才能晋升为成熟模式

## 三、模式库管理规则

**pattern-020**: 模式库按 domain + action 组织（.claude/skills/patterns/）
**pattern-021**: 每季度评估模式库覆盖率，清理 180 天未复用的模式
**pattern-022**: 模式描述必须包含：触发条件、执行步骤、τ 收益、验证状态
**pattern-023**: 模式使用后自动记录 usage_count，用于成熟度评估

## 四、Pattern Mining 与历史相似度的区分

**pattern-030**: 历史相似度（≥ 0.85）用于判断是否复用整个任务结果
**pattern-031**: Pattern Mining（≥ 0.6）用于判断哪些子步骤可复用
**pattern-032**: 两者并行执行，互不干扰，结果合并输出